{
  "// targets": "to see all targets run: nx show project entities-kdm-session --web",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "entities-kdm-session",
  "projectType": "library",
  "sourceRoot": "libs/entities/kdm-session/src",
  "tags": ["type:entities"],
  "targets": {
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tsc -b"],
        "cwd": "libs/entities/kdm-session"
      }
    }
  }
}
